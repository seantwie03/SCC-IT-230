/**
 * The key an embedding application provides to set `AsciinemaPlayer` speed.
 *
 * Course decks never provide it, so their recordings play in real time.
 */
export const IT230_RECORDING_SPEED_KEY = "it230-recording-speed";
